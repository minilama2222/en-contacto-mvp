---
import Base from '../layouts/Base.astro';
import { getContent } from '../utils/content';

const pages = getContent('psicoterapia');
---

<Base title="Psicoterapia" description="Servicios de psicoterapia individual, de pareja y familia">
  <article class="section-page">
    <header class="section-header">
      <h1>Psicoterapia</h1>
      <p>Centro profesional de Psicología Clínica dedicado desde 1987 principalmente a la psicoterapia individual y en grupo, de pareja y familia.</p>
    </header>

    <ul class="page-list">
      {pages.map(page => (
        <li>
          <a href={`/psicoterapia/${page.slug}`}>
            <span class="page-title">{page.title}</span>
          </a>
        </li>
      ))}
    </ul>
  </article>
</Base>

<style>
  .section-page {
    max-width: var(--width-content);
  }
  
  .section-header {
    margin-bottom: var(--space-xl);
    padding-bottom: var(--space-lg);
    border-bottom: 1px solid var(--color-border);
  }
  
  h1 {
    font-family: var(--font-heading);
    font-size: 2rem;
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-bottom: var(--space-sm);
  }
  
  .section-header p {
    font-family: var(--font-body);
    color: var(--color-text-secondary);
    line-height: 1.6;
  }
  
  .page-list {
    list-style: none;
    padding: 0;
  }
  
  .page-list li {
    border-bottom: 1px solid var(--color-border);
  }
  
  .page-list a {
    display: block;
    padding: var(--space-md) 0;
    text-decoration: none;
    color: var(--color-text);
  }
  
  .page-list a:hover {
    color: var(--color-text-secondary);
  }
  
  .page-title {
    font-family: var(--font-heading);
    font-size: 1rem;
    font-weight: 500;
  }
</style>
